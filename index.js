class Driver {
  constructor(name, joinDate) {
    this.name = name;
    this.joinDate = joinDate;
  }
  
  startDate() {
    new Date(this.joinDate);
  }
  
  yearsExperienceFromBeginningOf(year) {
    
  }
}